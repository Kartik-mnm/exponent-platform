# Copy this to .env.local for local development
# These are read by React at build time (must start with REACT_APP_)

# Your live backend URL (Render)
REACT_APP_API_URL=https://acadfee.onrender.com

# Once you have a custom domain, change to:
# REACT_APP_API_URL=https://api.exponent.app
